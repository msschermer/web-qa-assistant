import test from 'node:test';import assert from 'node:assert/strict';import {correlate,deterministicBrief} from '../packages/findings/correlate.js';
test('correlates canonical mismatch and performance context',()=>{const local={page:{url:'https://example.com/a',canonical:'https://example.com/a',title:'A',robots:'index'},findings:[]};const ctx={meta:{data:{snapshot:{pageMeta:{canonical:{resolved:'https://example.com/'},title:{value:'A'},robots:{meta:[{content:'index'}]}}}}},performance:{data:{monitored:true,threshold:70,mobile:48,desktop:92,mobileChange:-12}},wcag:{data:{mapping:{}}}};const out=correlate(local,ctx);assert(out.some(x=>x.ruleId==='correlation.canonical-mismatch'));assert(out.some(x=>x.ruleId==='performance.mobile-below-target'));assert(out.some(x=>x.ruleId==='performance.mobile-trend'))});
test('deterministic brief stays useful without AI',()=>{const text=deterministicBrief([{title:'Canonical mismatch',detail:'bad',category:'fix'},{title:'Missing label',detail:'bad',category:'fix'}]);assert.match(text,/canonical mismatch/i)});
